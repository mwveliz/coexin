{% extends 'base.html.twig' %}

{% block body %}
    <h1>Nuevo Codigo Arancelario </h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}
                    <a href="{{ path('codigoarancelario_index') }}" class="btn btn-danger" role="button">Cancelar</a>
                    <button type="button" class="btn btn-primary"  id="clear">Limpiar</button>
                    <button type="submit" class="btn btn-info">Enviar</button>    {{ form_end(form) }}

    <ul>
        <li>
            <a href="{{ path('codigoarancelario_index') }}">Back to the list</a>
        </li>
    </ul>
{% endblock %}
