{% extends 'base.html.twig' %}

{% block body %}
    <h1>Nuevo Codigo Arancelario </h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}
                    <a href="{{ path('codigoarancelario_index') }}" class="btn btn-warning"  role="button">Cancelar</a>
   
                    <button type="button" class="btn btn-primary"  id="clear">Limpiar</button>
                    <button type="submit" class="btn btn-info">Enviar</button>   

     
         
            <a href="{{ path('codigoarancelario_index') }}" class="btn btn-warning"  role="button">Cancelar</a>
    {{ form_end(form) }}
         
     
{% endblock %}
