{% extends 'base.html.twig' %}

{% block body %}
    <h1>Moneda list</h1>

    <table class="table table-striped table-bordered table-hover dt-responsive" width="70%" id="sample_2">
        <thead>
            <tr>
                <th>Descripcion</th>
                
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for moneda in monedas %}
            <tr>
                <td><a href="{{ path('moneda_show', { 'id': moneda.id }) }}">{{ moneda.descripcion }}</a></td>
                <td>{{ moneda.id }}</td>
                <td>
                     
                         
                            <a href="{{ path('moneda_show', { 'id': moneda.id }) }}">show</a>
                         
                         
                            <a href="{{ path('moneda_edit', { 'id': moneda.id }) }}" class="btn btn-info" role="button">Editar</a>
                         
                     
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

     
         
            <a href="{{ path('moneda_new') }}">Create a new entry</a>
         
     
{% endblock %}
