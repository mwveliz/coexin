{% extends 'base.html.twig' %}

{% block body %}
    <h1>RegistroPais</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ registroPai.id }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('registropais_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('registropais_edit', { 'id': registroPai.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
