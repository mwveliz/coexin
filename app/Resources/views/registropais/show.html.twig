{% extends 'base.html.twig' %}

{% block body %}
    <h1>RegistroPais</h1>

    

     
         
        <table class="table table-striped table-bordered table-hover dt-responsive" width="70%" id="sample_2">
<tbody>
     
         
            <tr>
                
              
            </tr>
        </tbody>
   </table>

     
         {{ form_start(delete_form) }}

     
         
            <a href="{{ path('registropais_index') }}" class="btn btn-warning" role="button">Cancelar</a>
         
         
            <a href="{{ path('registropais_edit', { 'id': registroPai.id }) }}" class="btn btn-info" role="button">Editar</a>
         
         
            
                <button type="submit" class="btn btn-danger">Eliminar</button>
            {{ form_end(delete_form) }}
         
     
{% endblock %}
