{% extends 'base.html.twig' %}

{% block body %}
    <h1>TipoEmpresa</h1>

    <table>
        <tbody>
            <tr>
                <th>Descripcion</th>
                <td>{{ tipoEmpresa.descripcion }}</td>
            </tr>
            <tr>
                <th>Id</th>
                <td>{{ tipoEmpresa.id }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('tipoempresa_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('tipoempresa_edit', { 'id': tipoEmpresa.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
