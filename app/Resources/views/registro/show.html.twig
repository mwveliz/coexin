{% extends 'base.html.twig' %}

{% block body %}
    <h1>Registro</h1>

    

     
         
        <table class="table table-striped table-bordered table-hover dt-responsive" width="70%" id="sample_2">
<tbody>
     
         
            <tr>
                <th>Fecha</th>
                <td>{% if registro.fecha %}{{ registro.fecha|date('Y-m-d') }}{% endif %}</td>
            </tr>
            <tr>
                <th>Codigoregistro</th>
                <td>{{ registro.codigoRegistro }}</td>
            </tr>
            <tr>
                
              
            </tr>
        </tbody>
   </table>

     
         {{ form_start(delete_form) }}

     
         
            <a href="{{ path('registro_index') }}" class="btn btn-warning" role="button">Cancelar</a>
         
         
            <a href="{{ path('registro_edit', { 'id': registro.id }) }}" class="btn btn-info" role="button">Editar</a>
         
         
            
                <button type="submit" class="btn btn-danger">Eliminar</button>
            {{ form_end(delete_form) }}
         
     
{% endblock %}
