{% extends 'base.html.twig' %}

{% block body %}
    <h1>DescripcionArancelaria list</h1>

    <table class="table table-striped table-bordered table-hover dt-responsive" width="70%" id="sample_2">
        <thead>
            <tr>
                <th>Descripcion</th>
                <th>Fecha</th>
                
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for descripcionArancelarium in descripcionArancelarias %}
            <tr>
                <td><a href="{{ path('descripcionarancelaria_show', { 'id': descripcionArancelarium.id }) }}">{{ descripcionArancelarium.descripcion }}</a></td>
                <td>{% if descripcionArancelarium.fecha %}{{ descripcionArancelarium.fecha|date('Y-m-d') }}{% endif %}</td>
              
                <td>
                     
                         
                            <a href="{{ path('descripcionarancelaria_show', { 'id': descripcionArancelarium.id }) }}">show</a>
                         
                         
                            <a href="{{ path('descripcionarancelaria_edit', { 'id': descripcionArancelarium.id }) }}" class="btn btn-info" role="button">Editar</a>
                         
                     
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

     
         
            <a href="{{ path('descripcionarancelaria_new') }}">Create a new entry</a>
         
     
{% endblock %}
