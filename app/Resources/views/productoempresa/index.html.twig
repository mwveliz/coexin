{% extends 'base.html.twig' %}

{% block body %}
    <h1>ProductoEmpresa list</h1>

    <table class="table table-striped table-bordered table-hover dt-responsive" width="70%" id="sample_2">
        <thead>
            <tr>
                
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for productoEmpresa in productoEmpresas %}
            <tr>
                <td><a href="{{ path('productoempresa_show', { 'id': productoEmpresa.id }) }}">{{ productoEmpresa.id }}</a></td>
                <td>
                     
                         
                            <a href="{{ path('productoempresa_show', { 'id': productoEmpresa.id }) }}">show</a>
                         
                         
                            <a href="{{ path('productoempresa_edit', { 'id': productoEmpresa.id }) }}" class="btn btn-info" role="button">Editar</a>
                         
                     
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

     
         
            <a href="{{ path('productoempresa_new') }}">Create a new entry</a>
         
     
{% endblock %}
